version: '3'

services:
  app:
    build:
      context: ./app
    ports:
      - "3000:3000"
    depends_on:
      - mysql

  mysql:
    image: mysql:5.7
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
    restart: always

  nginx:
    build:
      context: ./nginx
    ports:
      - "8080:80"
    depends_on:
      - app

